const { solve } = require('./solution');

describe('Problem XXX: <Problem Title>', () => {
  test('Example 1', () => {
    // Input:
    // Expected:
    expect(solve()).toEqual(undefined);
  });

  test('Example 2', () => {
    // Input:
    // Expected:
    expect(solve()).toEqual(undefined);
  });

  test('Edge case: empty input', () => {
    // Input:
    // Expected:
    expect(solve()).toEqual(undefined);
  });
});
